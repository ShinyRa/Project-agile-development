 const router = require('express').Router();

const {generateDatabase} = require('../misc/utils');


router.get('/database', (req, res) => {
    /* generateDatabase(); */
});


module.exports = router;